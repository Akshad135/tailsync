version: "3.8"

services:
  tailsync-brain:
    build: ./server
    container_name: tailsync-brain
    ports:
      - "8000:8000"
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
