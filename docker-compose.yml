version: "3.8"

services:
  tailsync:
    image: akshad/tailsync:latest
    container_name: tailsync
    ports:
      - "8000:8000"
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
